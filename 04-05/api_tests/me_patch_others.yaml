# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /me_patch for http method type PATCH in artillery framework
# RoostTestHash=6dd436357d
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_patch_others.csv"
    skipHeader: true
    fields:
      - "name"
      - "image"
      - "settings"
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test non-successful API responses"
    flow:
      - log: "Testing PATCH /me with invalid payload (missing 'name' field)"
      - patch:
          url: "/me"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 400
      - think: 1
      - log: "Testing PATCH /me with invalid authentication (missing ApiKeyAuth header)"
      - patch:
          url: "/me"
          headers: {}
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 401
      - think: 1
      - log: "Testing PATCH /me with invalid image data (not base64)"
      - patch:
          url: "/me"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "NotBase64ImageData"
            settings: "{{ settings }}"
          expect:
            - statusCode: 422
      - think: 1
      - log: "Testing PATCH /me with an incorrect URL"
      - patch:
          url: "/incorrect-url"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            name: "{{ name }}"
            image: "{{ image }}"
            settings: "{{ settings }}"
          expect:
            - statusCode: 404
      - think: 1
