# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /recipients/branches_put for http method type PUT in artillery framework
# RoostTestHash=63322ffb26
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "recipients_branches_put_others.csv"
    fields:
      - "cocNumber"
      - "cocBranchNumber"
      - "city"
      - "tradeName"
      - "phone"
      - "email"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Non-success API responses"
    flow:
      - log: "Sending a POST request with invalid cocNumber pattern"
      - post:
          url: "/recipients/branches"
          headers:
            Content-Type: "application/json"
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            cocNumber: "123"
            tradeName: "Test Trade Name"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Sending a POST request with missing required tradeName"
      - post:
          url: "/recipients/branches"
          headers:
            Content-Type: "application/json"
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            cocNumber: "12345678"
          expect:
            - statusCode: 422
            - hasProperty: "errors"
            - hasProperty: "errors.tradeName"
      - think: 1

      - log: "Sending a POST request with invalid email format"
      - post:
          url: "/recipients/branches"
          headers:
            Content-Type: "application/json"
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            cocNumber: "12345678"
            tradeName: "Test Trade Name"
            email: "invalid-email-format"
          expect:
            - statusCode: 422
            - hasProperty: "errors"
            - hasProperty: "errors.email"
      - think: 1

      - log: "Sending a POST request to an incorrect URL"
      - post:
          url: "/invalid/recipients/branches"
          headers:
            Content-Type: "application/json"
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            cocNumber: "87654321"
            tradeName: "Test Trade Name"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Sending a POST request without ApiKeyAuth header"
      - post:
          url: "/recipients/branches"
          headers:
            Content-Type: "application/json"
          json:
            cocNumber: "12345678"
            tradeName: "Test Trade Name"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1
