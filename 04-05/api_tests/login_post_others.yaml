# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /login_post for http method type POST in artillery framework
# RoostTestHash=3122292ee3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "login_post_others.csv"
    fields:
      - "client"
      - "email"
      - "otp"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test scenario for non-successful API requests"
    flow:
      - log: "Testing invalid email format"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            email: "invalid_email_format"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Testing missing required fields"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            email: "test@example.com"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Testing invalid client enum"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            email: "test@example.com"
            otp: "123456"
            client: "invalid_client_type"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Testing incorrect OTP"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            email: "test@example.com"
            otp: "wrong_otp"
            client: "mobile"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1

      - log: "Testing login with correct credentials but without authorization token"
      - post:
          url: "/login"
          headers:
            Content-Type: "application/json"
          json:
            email: "test@example.com"
            otp: "{{ otp }}"
            client: "{{ client }}"
          expect:
            - statusCode: 403
            - hasProperty: "message"
      - think: 1

      - log: "Testing invalid URL"
      - post:
          url: "/invalid_login_url"
          headers:
            Content-Type: "application/json"
          json:
            email: "test@example.com"
            otp: "123456"
            client: "mobile"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1
