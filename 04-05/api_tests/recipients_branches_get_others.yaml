# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /recipients/branches_get for http method type GET in artillery framework
# RoostTestHash=74a40da00e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "recipients_branches_get_others.csv"
    skipHeader: true
    delimiter: "^|^"
    fields:
      - "coc_number"
      - "coc_branch_number"
      - "trade_name"
      - "size"
      - "offset"
  environments:
    staging:
      target: "https://staging.example.com"
    production:
      target: "https://example.com"

scenarios:
  - name: "Scenario to test various non-successful API requests"
    flow:
      - log: "Sending a GET request with an invalid coc-number pattern"
      - get:
          url: "/recipients/branches?coc-number=invalidpattern"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1
      - log: "Sending a GET request with an out-of-range size parameter"
      - get:
          url: "/recipients/branches?size=100"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1
      - log: "Sending a GET request with a negative offset parameter"
      - get:
          url: "/recipients/branches?offset=-1"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1
      - log: "Sending a GET request without required ApiKeyAuth header"
      - get:
          url: "/recipients/branches"
          headers: {}
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1
      - log: "Sending a GET request to an incorrect URL"
      - get:
          url: "/recipients/branchess"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
