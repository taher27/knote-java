# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=4affc26394
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Attempting to delete an API key with an invalid ID format"
      - delete:
          url: "/apikeys/invalid-id-format"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Attempting to delete an API key without authorization header"
      - delete:
          url: "/apikeys/{{ id }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Attempting to delete an API key with an incorrect token"
      - delete:
          url: "/apikeys/{{ id }}"
          headers:
            ApiKeyAuth: "IncorrectToken"
          expect:
            - statusCode: 403
      - think: 1

      - log: "Attempting to delete an API key that does not exist"
      - delete:
          url: "/apikeys/00000000-0000-0000-0000-000000000000"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Attempting to access an invalid URL"
      - delete:
          url: "/invalid-url"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1
