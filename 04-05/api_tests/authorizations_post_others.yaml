# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /authorizations_post for http method type POST in artillery framework
# RoostTestHash=ed8c1b7bff
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "authorizations_post_others.csv"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-successful API requests scenario"
    flow:
      - log: "Attempting to authorize with invalid email format"
      - post:
          url: "/authorizations"
          headers:
            Content-Type: "application/json"
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "not_an_email"
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Attempting to authorize without providing email"
      - post:
          url: "/authorizations"
          headers:
            Content-Type: "application/json"
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json: {}
          expect:
            - statusCode: 400
            - hasProperty: "message"
      - think: 1

      - log: "Attempting to authorize with valid email but using a wrong API key"
      - post:
          url: "/authorizations"
          headers:
            Content-Type: "application/json"
            ApiKeyAuth: "incorrect_api_key"
          json:
            email: "validemail@example.com"
          expect:
            - statusCode: 401
            - hasProperty: "message"
      - think: 1

      - log: "Attempting to authorize with valid email but without API key"
      - post:
          url: "/authorizations"
          headers:
            Content-Type: "application/json"
          json:
            email: "validemail@example.com"
          expect:
            - statusCode: 403
            - hasProperty: "message"
      - think: 1

      - log: "Attempting to access an invalid URL"
      - post:
          url: "/invalid_url"
          headers:
            Content-Type: "application/json"
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "validemail@example.com"
          expect:
            - statusCode: 404
            - hasProperty: "message"
      - think: 1
