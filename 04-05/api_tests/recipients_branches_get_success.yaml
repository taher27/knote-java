# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /recipients/branches_get for http method type GET in artillery framework
# RoostTestHash=74a40da00e
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "recipients_branches_get_success.csv"
    fields:
      - "coc-number"
      - "coc-branch-number"
      - "trade-name"
      - "size"
      - "offset"
    skipHeader: true
    delimiter: "^|^"
  defaults:
    headers:
      ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Validate successful responses for /recipients/branches endpoint"
    flow:
      - log: "Sending GET request to /recipients/branches with valid coc-number"
      - get:
          url: "/recipients/branches?coc-number={{ coc-number }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "total"
            - hasProperty: "branches"
          capture:
            json: "$.branches[0].id"
            as: "first_branch_id"
      - think: 1

      - log: "Sending GET request to /recipients/branches with valid coc-branch-number"
      - get:
          url: "/recipients/branches?coc-branch-number={{ coc-branch-number }}"
          expect:
            - statusCode: 200
            - hasHeader: "page-size"
            - hasProperty: "total"
            - hasProperty: "branches"
          capture:
            json: "$.branches[0].tradeName"
            as: "first_branch_tradeName"
      - think: 1

      - log: "Sending GET request to /recipients/branches with valid trade-name"
      - get:
          url: "/recipients/branches?trade-name={{ trade-name }}"
          expect:
            - statusCode: 200
            - hasHeader: "page-offset"
            - hasProperty: "total"
            - hasProperty: "branches"
      - think: 1

      - log: "Sending GET request to /recipients/branches with pagination"
      - get:
          url: "/recipients/branches?size={{ size }}&offset={{ offset }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "total"
            - hasProperty: "branches"
      - think: 1
