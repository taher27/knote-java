# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=f1fc1d06f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Sending DELETE request with invalid UUID"
      - delete:
          url: "/credentials/invalid-uuid"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
      - think: 1

      - log: "Sending DELETE request with missing UUID"
      - delete:
          url: "/credentials/"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
      - think: 1

      - log: "Sending DELETE request without ApiKeyAuth header"
      - delete:
          url: "/credentials/{{ id }}"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
      - think: 1

      - log: "Sending DELETE request with expired ApiKeyAuth token"
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            ApiKeyAuth: "expired_token"
          expect:
            - statusCode: 403
            - hasHeader: "content-type"
      - think: 1
