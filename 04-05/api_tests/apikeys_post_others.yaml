# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /apikeys_post for http method type POST in artillery framework
# RoostTestHash=28a54f1579
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "apikeys_post_others.csv"
    fields:
      - "apiKey"
      - "sessionType"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Invalid API Usage Scenario"
    flow:
      - log: "Testing with invalid API key"
      - post:
          url: "/apikeys"
          headers:
            ApiKeyAuth: "InvalidApiKey"
          json:
            sessionType: 4
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
      - think: 1

      - log: "Testing with missing API key"
      - post:
          url: "/apikeys"
          headers:
            ApiKeyAuth: ""
          json:
            sessionType: 4
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
      - think: 1

      - log: "Testing with invalid session type"
      - post:
          url: "/apikeys"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            sessionType: "invalid"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
            - hasProperty: "message"
      - think: 1

      - log: "Testing with invalid URL"
      - post:
          url: "/invalidurl"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            sessionType: 4
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
      - think: 1
