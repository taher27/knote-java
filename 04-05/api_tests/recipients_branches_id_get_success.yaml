# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework
# RoostTestHash=061355b093
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "recipients_branches_id_get_success.csv"
    skipHeader: true
    delimiter: "^|^"
    fields:
      - "id"

scenarios:
  - name: "Validate successful response for GET /recipients/branches/{id}"
    flow:
      - log: "Starting GET request for /recipients/branches/{id}"
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasHeader: "api-version"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "id"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "email"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "phone"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "cocNumber"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "cocBranchNumber"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "tradeName"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "tradeNames"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "eoriNumber"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "deregistrationDate"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "createdAt"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - hasProperty: "updatedAt"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - matchesRegexp: "email"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - matchesRegexp: "cocNumber"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - matchesRegexp: "cocBranchNumber"
      - think: 1
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - matchesRegexp: "eoriNumber"
      - think: 1
