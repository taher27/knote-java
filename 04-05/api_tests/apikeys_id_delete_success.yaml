# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /apikeys/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=4affc26394
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "apikeys_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}
  defaults:
    headers:
      ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Validate successful API response for /apikeys/{id}"
    flow:
      - log: "Fetching API key details for ID {{ id }}"
      - get:
          url: "/apikeys/{{ id }}"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
      - think: 1
