# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=018c225aea
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Validate successful API responses for credentials"
    flow:
      - log: "Retrieving a specific credential by ID with expected success"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            ApiKey: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "id"
            - hasProperty: "format"
            - hasProperty: "credential"
            - hasProperty: "status"
            - hasProperty: "statusUpdatedAt"
            - hasProperty: "createdAt"
            - matchesRegexp: 
                key: "id"
                pattern: "\\b[0-9a-f]{8}-[0-9a-f]{4}-[1-2][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}\\b"
            - equals: 
                key: "status"
                value: "active"
      - think: 1
