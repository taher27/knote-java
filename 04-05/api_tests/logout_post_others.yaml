# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /logout_post for http method type POST in artillery framework
# RoostTestHash=64d33906e5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "logout_post_others.csv"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses Test"
    flow:
      - log: "Testing logout with invalid authentication token"
      - post:
          url: "/logout"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
      - think: 1

      - log: "Testing logout with missing authentication token"
      - post:
          url: "/logout"
          headers: {}
          expect:
            - statusCode: 403
            - hasHeader: "Content-Type"
      - think: 1

      - log: "Testing logout with invalid URL"
      - post:
          url: "/invalidlogout"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
      - think: 1

      - log: "Testing logout with invalid method (GET instead of POST)"
      - get:
          url: "/logout"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasHeader: "Content-Type"
      - think: 1

      - log: "Testing logout with invalid payload structure"
      - post:
          url: "/logout"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalidField: "invalidData"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
      - think: 1
