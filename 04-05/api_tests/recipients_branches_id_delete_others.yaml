# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /recipients/branches/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=1ce4bf6e3c
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "recipients_branches_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Sending a GET request with an invalid UUID"
      - get:
          url: "/recipients/branches/invalid-uuid"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
      - think: 2

      - log: "Sending a DELETE request without authorization header"
      - delete:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: ""
        expect:
          - statusCode: 401
      - think: 2

      - log: "Sending a POST request to a read-only endpoint"
      - post:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            invalidField: "invalidValue"
        expect:
          - statusCode: 405
      - think: 2

      - log: "Sending a GET request with a non-existent UUID"
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
      - think: 2

      - log: "Sending a GET request with a malformed UUID"
      - get:
          url: "/recipients/branches/123e4567-e89b-12d3-a456-426614174000X"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 400
      - think: 2

      - log: "Sending a GET request to an invalid URL"
      - get:
          url: "/invalid-url-path"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
      - think: 2
