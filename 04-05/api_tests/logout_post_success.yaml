# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /logout_post for http method type POST in artillery framework
# RoostTestHash=64d33906e5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "logout_post_success.csv"
    fields:
      - "username"
      - "sessionId"
    skipHeader: true
    delimiter: "^|^"
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"
      phases:
        - duration: 60
          arrivalRate: 5

scenarios:
  - name: "Logout scenario with success responses"
    flow:
      - log: "Starting logout request for a valid session"
      - post:
          url: "/logout"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            username: "{{ username }}"
            sessionId: "{{ sessionId }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: {"api-version": "1.0.0"}
      - think: 1
