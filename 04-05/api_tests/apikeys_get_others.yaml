# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /apikeys_get for http method type GET in artillery framework
# RoostTestHash=9e57a3b0dd
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "apikeys_get_others.csv"
    skipHeader: true
    fields:
      - "invalidApiKey"
      - "malformedPayload"
    delimiter: "^|^"

scenarios:
  - name: "Non-successful API request scenario"
    flow:
      - log: "Sending a GET request with an invalid API key"
      - get:
          url: "/apikeys"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - headerEquals: { "Content-Type": "application/json" }
      - think: 1

      - log: "Sending a GET request to an invalid URL"
      - get:
          url: "/invalidapikeys"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: { "Content-Type": "application/json" }
      - think: 1

      - log: "Sending a POST request with malformed payload"
      - post:
          url: "/apikeys"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            malformedPayload: "{{ malformedPayload }}"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - headerEquals: { "Content-Type": "application/json" }
      - think: 1

      - log: "Sending a DELETE request with an invalid API key"
      - delete:
          url: "/apikeys"
          headers:
            ApiKeyAuth: "{{ invalidApiKey }}"
          expect:
            - statusCode: 403
            - hasHeader: "Content-Type"
            - headerEquals: { "Content-Type": "application/json" }
      - think: 1
