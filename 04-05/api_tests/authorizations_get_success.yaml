# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /authorizations_get for http method type GET in artillery framework
# RoostTestHash=3b64f22044
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "authorizations_get_success.csv"
    skipHeader: true
    delimiter: "^|^"
    fields:
      - "authorizationId"
      - "name"
      - "email"
      - "createdAt"
      - "updatedAt"
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Validate successful API responses for /authorizations"
    flow:
      - log: "Starting GET request for /authorizations"
      - get:
          url: "/authorizations"
          headers:
            ApiKey: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
      - think: 1
      - get:
          url: "/authorizations"
          headers:
            ApiKey: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasHeader: "api-version"
      - think: 1
      - get:
          url: "/authorizations"
          headers:
            ApiKey: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasProperty: "total"
      - think: 1
      - get:
          url: "/authorizations"
          headers:
            ApiKey: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasProperty: "authorizations"
      - think: 1
      - get:
          url: "/authorizations"
          headers:
            ApiKey: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasProperty: "authorizations[0].id"
      - think: 1
      - get:
          url: "/authorizations"
          headers:
            ApiKey: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasProperty: "authorizations[0].name"
      - think: 1
      - get:
          url: "/authorizations"
          headers:
            ApiKey: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasProperty: "authorizations[0].email"
      - think: 1
      - get:
          url: "/authorizations"
          headers:
            ApiKey: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasProperty: "authorizations[0].createdAt"
      - think: 1
      - get:
          url: "/authorizations"
          headers:
            ApiKey: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - hasProperty: "authorizations[0].updatedAt"
