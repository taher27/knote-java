# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=cb128291e8
# 
# 

# ********RoostGPT********
config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "authorizations_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - flow:
      - log: "Starting test for removing an authorized account with valid UUID."
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
            api-version: "1.0.0"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
      - think: 1
