# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /recipients/branches/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=1ce4bf6e3c
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "recipients_branches_id_delete_success.csv"
    skipHeader: true
    fields:
      - "id"
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful API responses on recipient branch deletion"
    flow:
      - log: "Starting test for DELETE /recipients/branches/{id} with a valid UUID"
      - delete:
          url: "/recipients/branches/{{ id }}"
          headers:
            "{{ $processEnvironment.API_KEY_NAME }}": "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: 
                api-version: "1.0.0"
      - think: 1
      - log: "Starting test for GET /recipients/branches/{id} with a valid UUID"
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            "{{ $processEnvironment.API_KEY_NAME }}": "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "api-version"
            - headerEquals: 
                api-version: "1.0.0"
      - think: 1
