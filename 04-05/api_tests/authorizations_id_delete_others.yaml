# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=cb128291e8
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "authorizations_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Non-success API responses test"
    flow:
      - log: "Testing with missing authentication header"
      - delete:
          url: "/authorizations/{{ id }}"
          expect:
            - statusCode: 401

      - think: 1

      - log: "Testing with invalid authentication header"
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            ApiKeyAuth: "InvalidToken"
          expect:
            - statusCode: 403

      - think: 1

      - log: "Testing with invalid ID in the URL"
      - delete:
          url: "/authorizations/invalid-id-format"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

      - think: 1

      - log: "Testing with non-existent ID"
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

      - think: 1

      - log: "Testing with incorrect method (POST instead of DELETE)"
      - post:
          url: "/authorizations/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405

      - think: 1

      - log: "Testing with correct method but invalid URL"
      - delete:
          url: "/authorization/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404

      - think: 1
