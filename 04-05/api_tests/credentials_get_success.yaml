# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=66f52a572f
# 
# 

# ********RoostGPT********
config:
  target: "https://staging.example.com"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_get_success.csv"
    skipHeader: true
    fields:
      - "size"
      - "offset"
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Verify successful responses for credentials API"
    flow:
      - log: "Fetching credentials with valid size and offset parameters"
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Api-Key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals: { api-version: "1.0.0" }
            - hasProperty: "total"
            - hasProperty: "credentials"
            - hasProperty: "credentials[0].id"
            - hasProperty: "credentials[0].format"
            - hasProperty: "credentials[0].credential"
            - hasProperty: "credentials[0].status"
            - hasProperty: "credentials[0].statusUpdatedAt"
            - hasProperty: "credentials[0].createdAt"
      - think: 1
