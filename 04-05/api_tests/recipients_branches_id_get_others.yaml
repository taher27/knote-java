# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /recipients/branches/{id}_get for http method type GET in artillery framework
# RoostTestHash=061355b093
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "recipients_branches_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test various non-successful API requests"
    flow:
      - log: "Testing with an invalid UUID in the path"
      - get:
          url: "/recipients/branches/invalid-uuid"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1

      - log: "Testing with a missing UUID in the path"
      - get:
          url: "/recipients/branches/"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1

      - log: "Testing with a non-existent UUID in the path"
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1

      - log: "Testing with an invalid URL"
      - get:
          url: "/invalid/recipients/branches/{{ id }}"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1

      - log: "Testing without an API key"
      - get:
          url: "/recipients/branches/{{ id }}"
          expect:
            - statusCode: 401
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1

      - log: "Testing with an incorrect API key"
      - get:
          url: "/recipients/branches/{{ id }}"
          headers:
            ApiKey: "incorrect_key"
          expect:
            - statusCode: 403
            - hasHeader: "Content-Type"
            - headerEquals: {"Content-Type": "application/json"}
      - think: 1
