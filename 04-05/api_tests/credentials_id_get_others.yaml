# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=018c225aea
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_others.csv"
    delimiter: "^|^"
    skipHeader: true
    fields:
      - "id"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses Test"
    flow:
      - log: "Testing with invalid UUID in the URL"
      - get:
          url: "/credentials/invalid-uuid"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 400
      - think: 1

      - log: "Testing with missing UUID in the URL"
      - get:
          url: "/credentials/"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 404
      - think: 1

      - log: "Testing with valid UUID but without ApiKeyAuth header"
      - get:
          url: "/credentials/{{ id }}"
      - expect:
          - statusCode: 401
      - think: 1

      - log: "Testing with valid UUID but invalid ApiKeyAuth header"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            ApiKeyAuth: "invalid_token"
      - expect:
          - statusCode: 403
      - think: 1

      - log: "Testing with valid UUID and valid ApiKeyAuth header but incorrect HTTP method"
      - post:
          url: "/credentials/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 405
      - think: 1

      - log: "Testing with valid UUID and valid ApiKeyAuth header but incorrect URL"
      - get:
          url: "/credentialz/{{ id }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
      - expect:
          - statusCode: 404
      - think: 1
