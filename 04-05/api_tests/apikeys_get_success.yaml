# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /apikeys_get for http method type GET in artillery framework
# RoostTestHash=9e57a3b0dd
# 
# 

# ********RoostGPT********
config:
  target: "https://api.example.com"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "apikeys_get_success.csv"
    fields:
      - "id"
      - "createdAt"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "API keys success scenario"
    flow:
      - log: "Starting to list API Keys with a valid request expecting a 200 response"
      - get:
          url: "/apikeys"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - hasProperty: "total"
            - hasProperty: "apikeys"
            - matchesRegexp: "api-version: ^1\\.0\\.0$"
      - think: 1
