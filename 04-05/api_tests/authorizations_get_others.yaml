# ********RoostGPT********

# Test generated by RoostGPT for test karteKnoteTest using AI Type Open AI and AI Model gpt-4-1106-preview
# 
# Test generated for /authorizations_get for http method type GET in artillery framework
# RoostTestHash=3b64f22044
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "authorizations_get_others.csv"
    fields:
      - "invalidToken"
      - "invalidAccountId"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Responses"
    flow:
      - log: "Testing with invalid API key"
      - get:
          url: "/authorizations"
          headers:
            ApiKeyAuth: "invalidApiKey"
          expect:
            - statusCode: 401
            - hasHeader: "content-type"
      - think: 1

      - log: "Testing with missing API key"
      - get:
          url: "/authorizations"
          headers: {}
          expect:
            - statusCode: 403
            - hasHeader: "content-type"
      - think: 1

      - log: "Testing with invalid URL"
      - get:
          url: "/invalidauthorizations"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasHeader: "content-type"
      - think: 1

      - log: "Testing with invalid account ID in query parameter"
      - get:
          url: "/authorizations?accountId={{ invalidAccountId }}"
          headers:
            ApiKeyAuth: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasHeader: "content-type"
      - think: 1
